apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: vidme
spec:
  # Headless service required for StatefulSet DNS (postgres.vidme.svc.cluster.local)
  clusterIP: None
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
